/**
 * Interface for verification tokens.
 *
 * @interface
 * @property {string} email - The email associated with the verification token.
 */
export interface IVerificationToken {
    email: string;
}
